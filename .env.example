# Backend
HOST=<backend-host>
PORT=<backend-port>
# Comma-separated allowed browser origins (optional in development)
# Example: https://app.company.com,https://admin.company.com
CORS_ORIGIN=
JSON_LIMIT=512kb

# Frontend
# Empty keeps same-origin API calls (recommended for production behind nginx)
VITE_API_BASE_URL=
# Optional override for Vite dev proxy target
VITE_DEV_API_TARGET=http://localhost:3000
